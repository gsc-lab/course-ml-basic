"""
TITLE: 4가지 lr 스케줄 종합 벤치마크
DIFFICULTY: hard
TAGS: gradient-descent, minibatch, learning-rate, warmup, decay, scheduler, application
EVAL: script

DESCRIPTION:
지금까지 배운 4가지 학습률 스케줄을 같은 데이터/조건에서 비교 평가하세요.

이 문제는 "스케줄러를 함수 객체로 받아 동일한 학습 루프에 주입" 하는 패턴과,
"수렴 속도 / 안정성 / 최종 loss" 라는 다중 메트릭으로 비교하는 분석 능력을
익히는 것이 목적입니다.

  ┌─────────────────────────────────────────────────────────────┐
  │ 1. lr_constant      : base_lr 그대로 끝까지                  │
  │ 2. lr_warmup_only   : 0 → base_lr 까지 올린 뒤 base_lr 유지  │
  │ 3. lr_decay_only    : base_lr 에서 시작해 0 까지 선형 감소   │
  │ 4. lr_warmup_decay  : 삼각형 (warmup ↗ → decay ↘)           │
  └─────────────────────────────────────────────────────────────┘

평가 지표:
  · loss ≤ threshold 처음 도달한 epoch    (수렴 속도)
  · 마지막 50 epoch 의 평균 loss            (정확도)
  · 마지막 50 epoch 의 스윙폭 (max - min)  (안정성)
  · final loss                            (최종 안착 지점)

요구 사항:
  - 아래 데이터셋(x_data, y_data)은 변경하지 마세요. (적당한 노이즈가 들어 있음)
  - 4가지 스케줄러 함수와 학습 함수를 직접 작성합니다.
  - 4가지 스케줄을 동일한 학습 루프에 주입하여 위 메트릭을 표로 비교 출력하세요.

생각해보기:
  1. warmup 을 추가하면 수렴이 느려지지만 final loss 는 거의 같다.
     그러면 warmup 은 언제 효과가 큰가?
  2. 실무에서는 왜 warmup+decay 조합을 표준으로 쓸까?
"""
# META_TESTS:
# - stdin: ""
#   expected_stdout: ""

import random

# ============================================================
# 데이터셋 (노이즈 있는 20개) - 변경 금지
# ============================================================
random.seed(0)
x_data = [i for i in range(1, 21)]
y_data = [0.5 * x + 2 + random.uniform(-1.0, 1.0) for x in x_data]


# ============================================================
# 학생 구현 영역
#   - 4가지 스케줄러 함수와 학습 함수를 직접 작성하세요.
#   - 메트릭 비교를 표 형태로 출력하세요.
# ============================================================
